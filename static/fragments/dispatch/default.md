# Default dispatch configuration

- Child-task model: `gpt-5.6-luna`
- Reasoning effort: `medium`
- Per-task creation confirmation: `false`

Pass model and reasoning through the task-creation tool, not through prose in the child prompt. The user's acceptance of the default authorizes creation only inside the current commander project and does not expand filesystem, external-service, publication, deletion, payment, or approval scope.
