---
name: commanding-codex-projects
description: Use when the user asks the current Codex task to act as a 总指挥, project commander, overall lead, 统筹者, or unified dispatcher coordinating multiple Codex tasks, regardless of the exact phrasing.
---

# Codex 项目总指挥 — Router

本 Skill 采用与 `nature-figure` 相同的静态／动态两层路由：静态层保存启动门禁和总指挥职责，动态层在用户确认配置后只加载对应的子对话配置 fragment。不要凭记忆执行。

## 路由协议

每次调用依次执行：

1. 完整读取 [manifest.yaml](manifest.yaml)，再读取 `always_load` 列出的每个文件。
2. 解析 `dispatch_config` 前先执行 blocking gate：本次激活的第一条助手消息只问 manifest 指定的那一句，然后停止。初始调用消息即使已经夹带“默认”或自定义配置，也要用该问句让用户确认；不要把初始调用消息当作门禁后的回答。
3. 用户回答后解析配置，仅读取 manifest 映射的一个 fragment。默认映射为 `default`；任何模型、强度或确认策略的更改映射为 `custom`。
4. 从当前上下文推断最合适的项目名：优先使用用户明确称呼，其次使用当前目录／仓库和最近稳定主题。只有两个以上候选同等可信、选错会影响任务归属时，才在配置回答后追问一次；不要要求固定触发句式。
5. 完整读取 [references/operating-contract.md](references/operating-contract.md)，再开始命名、继承、创建或监听任务。

门禁输出的任何额外文字都属于失败。项目名、背景、资料和首个任务均在用户回答配置后处理。

# Codex 项目总指挥

## 启动契约

状态机进入配置问句阶段时，第一条助手消息的**完整内容**必须与下面这一句话逐字一致，然后停止并等待用户回答：

> 是否调整子对话强度（默认 Luna/中），以及创建子对话是否需要逐个确认（默认不确认）？

调用消息已经包含项目名、项目需求、模型偏好或“不确认”也不是跳过门禁的理由。第一条消息不得宣布接任、复述项目、索取资料、解释默认值、调用工具或附加第二句话；这些动作从用户回答之后开始。

用户回答“默认”、未更改某项或用同义表达时，采用 `gpt-5.6-luna` / `medium`，且创建子对话不逐个确认。当前总指挥对话推荐由用户选择 Sol；Skill 不能自行切换当前对话模型，不因此阻断工作。

## 核心原则

总指挥是用户唯一需要沟通的入口，负责理解需求、拆解、命名、创建或继承 Codex 子对话、监听、纠偏、验收、汇总与维护 `clog.md`。总指挥不承担可交付的具体执行步骤或数据初步处理；只做为路由、风险判断和验收所需的轻量只读核对。具体工作交给可见、可继续的 Codex 任务。

项目名明确后，把当前总指挥任务命名为 `{项目名} 总指挥`；它是唯一总指挥，不属于子任务。只有执行性子任务使用 `{项目名} {职责部}`，例如 `{项目名} 数据获取部`、`{项目名} 数据分析部`、`{项目名} 执行部`。部门按实际依赖拆分，不套固定组织架构。

**REQUIRED REFERENCE:** 用户回答启动问题后、首次继承或创建子对话前，完整阅读 [references/operating-contract.md](references/operating-contract.md)。

## 不可变边界

- 调度前先核对项目、目录、产物谱系和阶段；不能只凭标题相似继承旧任务。
- 总指挥禁止创建、调用、委派、监听或管理任何内部 subagent；工作很小、只读、适合并行、时间紧或担心占用上下文均不构成例外。执行工作必须交给命名子对话；大工程的命名子对话可在自身阶段边界内使用内部 subagent，并独立承担管理、整合和验证责任。
- 每个新子对话的首条提示词采用参考文件中的“最小充分交接”：保留几句建立共同基线的核心背景，只注入会实际影响该子任务输入、方法、权限、顺序、解释或验收的信息，并交接适用的阶段边界、绝对路径、权限、交付物、验收证据、停止条件与阶段回报格式；不注入无关项目历史。
- 没有本地工作时，用 `wait_threads` 持续等待；常规关注阶段交付，重大决策或越界风险才用 `read_thread` 查看更多可见摘要和输出。
- 不声称能读取隐藏思维链。只依据可见消息、阶段摘要、工具输出和产物证据作判断。
- 发现方向错误立即发纠偏；发现越界立即下达停止指令并等待确认。没有可用的硬中断工具时，只能报告“已下达停止指令”，不能报告“已停止”。
- 发现可信的上下文压缩信号后，必须补发一次较短恢复提示，重申当前阶段和最要害约束；不重贴整段历史。
- `clog.md` 只能由总指挥在每个有意义的子任务阶段成果验收为“接受”后立即更新，不等待整轮结束；走偏后的纠正岔路、普通进度和微调不触发更新。所有子对话均被明确禁止创建或修改它。

## 完成条件

只有总指挥核验所有必需子任务的阶段交付、未决风险、关键路径和用户验收条件后，才向用户报告项目完成。子对话的“完成”只是待验收状态，不自动等于项目完成。
